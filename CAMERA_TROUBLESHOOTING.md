# Camera Access Troubleshooting | حل مشاكل الوصول إلى الكاميرا

## المشكلة: "فشل الوصول إلى الكاميرا"

### الحلول المطبقة ✅

1. **إصلاح Permissions-Policy Header**
   - تم تحديث `next.config.js` للسماح بالوصول إلى الكاميرا
   - تغيير من `camera=()` إلى `camera=(self)`

2. **تحسين معالجة الأخطاء**
   - رسائل خطأ محددة حسب نوع الخطأ
   - محاولة تلقائية بإعدادات بسيطة عند الفشل
   - فحص HTTPS/localhost تلقائياً

3. **إضافة Fallback**
   - محاولة بإعدادات بسيطة إذا فشلت الإعدادات المثالية
   - إعادة محاولة تلقائية

## حلول إضافية | Additional Solutions

### 1. التحقق من HTTPS/localhost

الكاميرا تتطلب HTTPS في الإنتاج، لكن تعمل على localhost:

```bash
# Development (localhost works)
http://localhost:3000 ✅

# Production (needs HTTPS)
https://yourdomain.com ✅
http://yourdomain.com ❌
```

### 2. إعدادات المتصفح

#### Chrome/Edge:
1. اضغط على 🔒 بجانب العنوان
2. اختر "إعدادات الموقع"
3. تأكد من أن "الكاميرا" مسموحة

#### Firefox:
1. اضغط على 🔒 بجانب العنوان
2. اختر "أذونات"
3. تأكد من السماح بالكاميرا

#### Safari:
1. Safari → Preferences → Websites → Camera
2. تأكد من السماح للموقع

### 3. إغلاق التطبيقات الأخرى

الكاميرا يمكن استخدامها من تطبيق واحد فقط:
- أغلق Zoom, Teams, Skype
- أغلق أي تطبيق يستخدم الكاميرا
- أعد تحميل الصفحة

### 4. إعادة تعيين الأذونات

#### Chrome:
```
chrome://settings/content/camera
```
- احذف الموقع من القائمة
- أعد تحميل الصفحة
- اسمح بالوصول عند الطلب

#### Firefox:
```
about:preferences#privacy
```
- ابحث عن "Permissions"
- احذف الأذونات للموقع
- أعد تحميل الصفحة

### 5. التحقق من إعدادات النظام

#### Windows:
1. Settings → Privacy → Camera
2. تأكد من تفعيل "Allow apps to access your camera"
3. تأكد من تفعيل "Allow desktop apps to access your camera"

#### macOS:
1. System Preferences → Security & Privacy → Camera
2. تأكد من السماح للمتصفح

### 6. اختبار الكاميرا

افتح console في المتصفح (F12) وجرب:

```javascript
navigator.mediaDevices.getUserMedia({ video: true })
  .then(stream => {
    console.log("Camera works!", stream);
    stream.getTracks().forEach(track => track.stop());
  })
  .catch(error => {
    console.error("Camera error:", error);
  });
```

## أنواع الأخطاء الشائعة | Common Error Types

### NotAllowedError / PermissionDeniedError
**السبب**: المستخدم رفض الوصول أو المتصفح منع الوصول

**الحل**:
- السماح في إعدادات المتصفح
- إعادة تعيين الأذونات
- استخدام متصفح آخر

### NotFoundError / DevicesNotFoundError
**السبب**: لا توجد كاميرا متصلة

**الحل**:
- تأكد من وجود كاميرا
- تأكد من اتصال الكاميرا
- جرب كاميرا خارجية

### NotReadableError / TrackStartError
**السبب**: الكاميرا مستخدمة من تطبيق آخر

**الحل**:
- أغلق التطبيقات الأخرى
- أعد تشغيل الكاميرا
- أعد تشغيل الكمبيوتر

### OverconstrainedError
**السبب**: الكاميرا لا تدعم الإعدادات المطلوبة

**الحل**:
- الكود يحاول تلقائياً بإعدادات بسيطة
- أو استخدم كاميرا أخرى

### SecurityError
**السبب**: الموقع ليس HTTPS (في الإنتاج)

**الحل**:
- استخدم HTTPS
- أو استخدم localhost للتطوير

## اختبار الوصول | Testing Access

### 1. اختبار بسيط

افتح console وجرب:
```javascript
navigator.mediaDevices.enumerateDevices()
  .then(devices => {
    const videoDevices = devices.filter(d => d.kind === 'videoinput');
    console.log('Available cameras:', videoDevices);
  });
```

### 2. اختبار الوصول

```javascript
navigator.mediaDevices.getUserMedia({ video: true })
  .then(stream => alert('Camera works!'))
  .catch(error => alert('Error: ' + error.name));
```

## إذا استمرت المشكلة | If Problem Persists

1. **جرب متصفحاً آخر**
   - Chrome (الأفضل)
   - Firefox
   - Edge

2. **تحقق من Console**
   - اضغط F12
   - ابحث عن أخطاء
   - أرسل رسالة الخطأ

3. **أعد تشغيل المتصفح**
   - أغلق جميع النوافذ
   - أعد فتح المتصفح

4. **أعد تشغيل الكمبيوتر**
   - أحياناً يحل مشاكل الكاميرا

## ملاحظات | Notes

- الكاميرا تعمل على `localhost` حتى بدون HTTPS
- في الإنتاج، يجب استخدام HTTPS
- بعض المتصفحات أكثر صرامة من غيرها
- الكود يحاول تلقائياً بإعدادات مختلفة عند الفشل

